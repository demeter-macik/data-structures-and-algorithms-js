const {LinkedListWithInsertSorting} = require('./insert-sort.js');
const {DoublyLinkedList} = require('../../data-structures/doubly-linked-list.js');
const {randomlyShuffleArray} = require('../../utils.js');

test('should sort array', () => {

});
